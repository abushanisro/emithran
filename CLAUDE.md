# eMithran — Project Guidance

## Product development strategy

eMithran is developed manufacturing-domain by manufacturing-domain, one at a time — not as a universal manufacturing framework built all at once.

**Current priority: Sheet Metal.**

The Sheet Metal domain must reach production readiness before *new* work begins on the next manufacturing domain (Machining, then Injection Molding, in that order).

**Correction (2026-08-29):** this "hasn't started" framing was stale. Real, tested, live-wired costing engines already exist for both later domains — `cost-cnc-engine.ts` (Machining/CNC, 43 tests) and `cost-injection-molding-engine.ts` + its `injection-molding/` subfolder (`process-tree.ts`, `routing-engine.ts`, `cycle-time.ts`, `machine-selector-im.ts` — already through its own internal "Phase 4" with real physics models: Menges cooling, flow-length fill, gate-freeze pack). Both are wired into `bom-items.service.ts` and share Sheet Metal's exact same kernel/output shape (`MHRRateInput`, `computeSustainability`, `CostSummaryDto`, `ProcessLineCost` from `cost-engine.ts`) and the same "route first, cost the route" pattern. Raw reference data for both domains is also already staged on disk (`memory/machining/`, `memory/Injection/`), ahead of this file's stated sequencing. The priority ordering above still holds for *deliberate, dedicated* domain buildout and freezing — but do not assume "Machining/Injection Molding" means empty/unbuilt when touching shared kernel code (`cost-engine.ts`, `default-rates.ts`, DTOs) — changes there affect three live domains, not one.

**Platform architecture mandate (2026-08-31):** eMithran is being built as a long-lived manufacturing platform, not a Sheet-Metal feature patch. Every manufacturing-domain change (new process, new engine, routing/capability logic) must be checked against 20 explicit architectural rules and this layering — `CAD → CAD Feature Extraction → Canonical Manufacturing Feature Model → Manufacturing Capability/Decision Engine → Routing Engine → Process Engine Registry → Machine Capability Matching → Cost Calculation → Versioned Result → API → UI` — before being added. Full rule list lives in this session's memory (`feedback_platform_architecture_boundaries.md`); the acceptance test is: a future engineer should be able to add a new manufacturing process, machine type, feature, or routing template without modifying unrelated processes or creating another parallel representation of manufacturing knowledge. A 3-agent audit found 9 categories of violation, sequenced into remediation phases — audit before implementing new functionality on top of a known crack, don't patch around it.

**Phase 1 — engine registry unification, closed (2026-08-31):** The most severe finding was a live correctness bug: the primary/default quote path (`computeCostSummary()` in `cost-engine.ts`) reimplemented cost math inline instead of calling the registered `ManufacturingProcessEngine` instances the secondary route-comparison path (`getRouteComparison()`) used — so the same operation on the same part could produce two different dollar totals depending on which code path ran (confirmed for Laser Cutting, and for Press Brake/Deburr/Tapping/Burring's hand-rolled `getRouteComparison()` blocks, which had no labor/QA-inspection-sampling/yield-loss cost at all). Fixed by promoting `eMithranTerms()` (the platform's one real, generic cost-composition core) into the shared `engine-kernel.ts`, migrating all 7 registered sheet-metal cutting/forming engines onto it, extracting the remaining 8 secondary-op inline blocks (`press-brake-engine.ts`, `deburring-engine.ts`, `tapping-engine.ts`, `hole-extrusion-engine.ts`, `counterboring-engine.ts`, `countersinking-engine.ts`, `pem-insertion-engine.ts`, `reaming-engine.ts`) into registered engines of their own, and rewiring both `computeCostSummary()` and `getRouteComparison()` to call these same engines instead of duplicating math. `ManufacturingProcessEngine` was generalized (`TContext`/`TResult`/`TGeometry`/`TCapabilityResult`, all defaulted so the original 15 sheet-metal engines needed zero changes) and thin conformance wrappers were added for CNC (`cnc-milling-registry-engine.ts`, `cnc-turning-registry-engine.ts`), Injection Molding (`injection-molding-registry-engine.ts`), Inspection (`inspection-registry-engine.ts`), and Surface Treatment (`surface-treatment-registry-engine.ts`) — no formula rewrites, every wrapper calls the existing real function unchanged, so this now holds across all three domains, not just Sheet Metal. `MachineClass` gained a `surface_treatment` entry it was missing despite `cost-surface-treatment.ts` already emitting that literal. Deliberately NOT done in this phase (real gaps, not oversights — see the next phases below): the existing CNC/IM/Inspection call sites in `bom-items.service.ts` still call their real functions directly rather than through the registry (no divergence bug existed there to fix, and swapping ~10 scattered call sites in an 8,000-line file for zero bug-fix benefit would have been unrelated refactoring); canonical process/machine IDs are still free-text strings platform-wide; the three incompatible "route" representations (`RouteStep`/`IMProcessTree`/`OperationLine`) are still unreconciled; frontend hardcoding (scoring weights, `WORKFLOW_KB`, material classifiers) is untouched; no location/currency/effective-date/versioning work. 582+ backend tests green, full `tsc`/NestJS build clean throughout.

Production readiness means the full pipeline works **dynamically** across the domain's real scenarios, not through hard-coded per-scenario paths:

```
CAD / STEP
  → Geometry extraction
  → Feature extraction
  → Material + thickness
  → Operations
  → Process selection
  → Machine capability
  → Machine selection
  → DFM
  → Cycle/setup calculations
  → Material + labor + machine + tooling + consumables
  → Cost
  → Explainable calculation trace
```

Two parts (e.g. 3mm SS304 with 6 bends and 14 holes vs. 2mm AL5052 with 4 bends and 8 holes) must automatically produce different features, operations, process candidates, machine candidates, cycle times, costs, and DFM results — never a special case coded per part or per "scenario."

Do not optimize for passing predefined scenarios through hard-coded logic. The system must derive behavior from real inputs: geometry, material, thickness, detected features, resolvable operations, real process capabilities, real machine constraints, and real costing parameters. When a real input needed to derive behavior correctly doesn't exist yet (missing CAD capability, missing reference data, missing linkage between two subsystems), that is a capability gap to document and flag — not a value to fabricate or a threshold to guess just to make a check pass.

### Sheet Metal readiness checklist

**CAD**
- [x] STEP ingestion
- [x] thickness extraction
- [x] bend extraction
- [x] continuous rolled/curved-sheet recognition, distinct from a discrete press-brake bend — `sheet_metal/features/rolled_form.py`, wired into `extract()`/`feature_graph_v2`, 15 real B-Rep tests (2026-09-09). A single cylindrical face sweeping past 180° is a confident `'recognized'` detection (a press brake cannot wrap material past a half-turn in one hit); a large-radius face sweeping within press-brake range stays `'ambiguous'`, disclosed not guessed. Closes the routing bug this was built to fix (830-001720-00 misrouted to "3 Roll Bending" for four ordinary discrete bends) with real positive evidence, not just a negative gate — see the Cost section entry below.
- [x] hole extraction
- [x] slot/other real features (cut_profile, extruded_flange, thin_web)
- [x] perforation pattern detection (repeated-hole regions, distinguished from incidental same-diameter holes via real `sm_reference_data` thresholds `defaultNumIdentHolesPerforating`/`defaultNumNearbyIdentHolesPerforating`) — `sheet_metal/features/perforation.py`, wired into `extract()`/`feature_graph_v2`, 12 real tests incl. one real-OCC end-to-end (2026-09-02)
- [x] formed-feature (dimple/emboss) recognition, scoped to shallow blind cylindrical cavities — FIXED (2026-09-10). `forming_spike.py`'s own honest limit stands (a shallow blind cavity alone is indistinguishable from an ordinary blind hole), but `sheet_metal/features/formed_feature.py` adds the missing positive test on top: the sheet's OPPOSITE face has its own local void (an inner wire) at the same footprint only when material was worked from both faces — a blind hole leaves the back untouched. Real topology, not a fabricated threshold; classification of a confirmed candidate (shallow/deep) uses real sourced multipliers from `memory/sheetmetal/sheet_metal_variables.json` (`shallowEmbossDepthMultiplier`, `deepDimpleMultiplier`). 7 real B-Rep tests, wired into `extract()`. Does not extend forming_spike's own depth/shape scope (still cylindrical, still 0.2–0.9× thickness) — a deep paired-offset boss remains out of reach, as documented there.
- [x] lance detection (partial cut, material remains attached, displaced flap) — FIXED (2026-09-10). `lancing_spike.py`'s own candidate discovery is unchanged; `sheet_metal/features/lance.py` adds the missing positive test: a genuine lance's base panel encloses the hinge as a real interior hole (an inner wire), while an ordinary separate bent tab's hinge sits on the panel's outer boundary instead. Real topology (same wire-index convention `_face_breakdown` already uses), zero fabricated threshold. Core topological primitive verified against real B-Rep (4 tests); full-pipeline verification against a genuine fused lance solid remains open (constructing that solid is itself nontrivial OCC work, disclosed not attempted — same gap `lancing_spike.py`/`gusset_spike.py` already named).
- [x] Restriking / Setting / Coining / Scoring confirmed to need NO new CAD work (2026-09-04) — cross-checked `process_operations.json`'s `Restriking//Form|MultiBend|ShearForm`, `Setting//Form|StraightBend`, `Coining//Blank|ComplexHole|SimpleHole`, `Scoring//Form|StraightBend` taxonomy against real `sm_reference_data` (`enableRestrikingStraightCurvedBends`, `bendFlapSizeRestrikeThreshold`, `enableCoiningFlangedHoles`, migration 479): all four are real, sourced PROCESS toggles/thresholds applied to an ALREADY-DETECTED bend/hole/blank feature (how many press hits/which station finishes it), never producing final-part geometry distinct from the underlying Form/StraightBend/Hole — same pattern as the already-closed Flanging finding above. Scoring has no sourced definition beyond the same taxonomy shape as the other three (no contradicting evidence found), so grouped with them absent a reason not to. Nothing to detect, nothing to wire in.
- [ ] gusset-corner detection (formed material closing a bend corner, vs. an open relief cut) — investigated (2026-09-04), NOT shippable: unlike Restriking/Setting/Coining/Scoring above, `defaultGussetStrategy`/`gussetBendComp1`/`gussetBendComp2` (migration 479) and the `BendHasGussets` DFM advice rule (migration 483) confirm a gusset is a real, distinct MATERIAL decision at a corner where two bend lines meet near-perpendicular — genuinely different final geometry (closed vs. open), not a process toggle on existing geometry. Feasibility spike (`sheet_metal/features/gusset_spike.py`, 11 tests, synthetic-dict verification only) pairs bends by near-perpendicular axes + close endpoints (real vector math on `raw_cylinders_full`'s axis/centroid/length fields), then checks for a real bridging face via `bend_relationships.py`'s adjacency technique. The OPEN-corner case (no bridging face) is a genuine, confident negative (`recognition_status='recognized'`) — but the CLOSED-corner case can't be told apart from an unrelated design chamfer/fillet/bracket face sitting at the same location, so every candidate gusset stays `'ambiguous'`. Deliberately NOT wired into `extract()`. No real-B-Rep fixture attempted (same disclosed gap as forming/lancing spikes) — that verification remains open.
- [x] "Flanging" (Laser Punch/Plasma Punch/Turret Press routes) confirmed to need NO new CAD work — cross-checked `process_operations.json`'s `Flanging//ComplexHole|SimpleHole` taxonomy against live `sm_reference_data` (`enableCoiningFlangedHoles`, `CheckIfFlanged`) and the existing `extrudedFlangeCount` doc comment: it's hole-flanging, already fully covered by the existing `extruded_flange_count` detector + `HoleExtrusionEngine`/"Hole Extrusion (Burring)" cost engine (real, tested, live). Only remaining gap is a naming/alias link, already scoped under the Phase 2a process-taxonomy plan — not CAD work.
- [x] bend-to-bend / bend-to-flange relationships (signed fold direction) — FIXED (2026-09-10). `bend_relationships.py`'s `compute_bend_flange_relationships()` was real, tested (Phase 5, 2026-08-21) but had zero call sites in `extract()` — computed, never wired. Now called from `extract()` using the real per-physical-bend `face_ids` `feature_graph_v2` already builds, exposed as `bend_flange_relationships` (flange_width_mm, fold_relative_orientation — real facts, still no hem/return-flange verdict, that stays DFM's job per the module's own docstring). Wired through to the backend (`auto-fill.service.ts`); no consumer reads it yet.
- [x] flat-pattern information (outline, area, K-factor/bend allowance)
- [x] stable topology provenance — FIXED (2026-09-10). Face identity was purely `TopExp_Explorer` enumeration-order ordinals (feature_extractor.py's own docstring: "NOT stable across STEP regeneration"). `shared/stable_face_id.py` adds a content-based id (SHA256 of surface type + real area + real centroid, rounded) per face, additive alongside the existing ordinal — every current consumer (bend/hole/formed-feature/lance detection) is unchanged. Verified against real B-Rep: the SAME box built two structurally different ways (a single `BRepPrimAPI_MakeBox` vs. two halves fused with `BRepAlgoAPI_Fuse` + `ShapeUpgrade_UnifySameDomain`, which genuinely reorders OCC's enumeration) produces matching stable ids per real face. Wired into `extract()` as `feature_graph_v2.metadata.stable_face_ids`.
- [x] deterministic, versioned, normalized feature contract — FIXED (2026-09-10). `shared/feature_contract.py` had real, designed infrastructure (`FEATURE_CONTRACT_VERSION = "1.0"`, `build_normalized_feature()`) with zero real call sites — confirmed by grep, designed-but-dormant, not "in progress". Now wired into `extract()`: one normalized record per real `feature_graph_v2` occurrence (bends, holes, formed-features, lances, rolled-forms, perforation, slots), every field traced to an already-computed fact, additive to the existing ad-hoc `feature_graph_v2` shape (not a replacement). Enriched with `source_face_stable_ids` via the stable-face-id map above. Exposed as `feature_graph_v2.normalized_features` + `feature_graph_v2.metadata.feature_contract_version`. Verified end-to-end on a real two-bend U-channel run through the full production `extract()` pipeline (`test_feature_contract_wiring.py`) — not just unit-level.

**Process** (existence ≠ production readiness — each needs real operations, constraints, machine capabilities, and costing behavior, not just a taxonomy row)
- [x] Fiber Laser, Laser — plus, FIXED (2026-09-10): "3D Laser Cutting Machine" (15 machines) was COALESCEd into fiber_laser by migration 569, contaminating that pool despite a structurally different real capability schema (memory/sheetmetal/machine/machine_library.json: Fiber Laser has 5 real per-material max-thickness fields; "3D Laser Cutting Machine" uniquely carries a real bed_height_mm/Z-axis field, confirming it isn't even a flat-sheet machine). Fix: `ThreeDLaserCuttingEngine` (`laser_3d`, `laser-cutting-engine.ts`) reuses the same real cost formula as Fiber/CO2 (no technology-specific assumption in `computeLaserCuttingCost`), its own registered route; real conservative capability defaults sourced from the real per-category minimum bed X/Y/Z (`seed-registry.ts`). **Corrected same day**: an initial draft also split "Laser Cutting Machine" (24 machines, e.g. "Cincinnati CL 850") into a brand-new `laser_cut` class/`LaserCutEngine`, reasoning it had no per-material thickness fields like Fiber Laser's — that was wrong, caught before the migration was ever run. `memory/sheetmetal/machine/india_base.json` independently names this exact same 24-machine set "CO2 Laser Cutter" (confirmed name-for-name, only OCR-level spelling differences) — it's the same real pool `co2_laser` (and "Quattro", migration 456) already covers, not a fourth class. `LaserCutEngine`/`laser_cut` were removed; migration 722 instead routes the real "Laser Cut" operation and "Laser Cutting Machine" `mhr_records` fleet to `co2_laser`, and `co2_laser` gained the real conservative bed X/Y floor. Does not conflict with migration 715's separate "Laser Cut"/"Fiber Laser Cut" duplicate-name dedup (calculator/formula choice, not a machine-pool claim). **User decision (2026-09-10):** the human-facing name for this real 24-machine pool stays "Laser Cut"/"Laser Cutting Machine" (matching the live catalog operation and machine_library.json's own category label) — never "CO2 Laser Cutter"/"CO2 Laser"; `co2_laser` remains only the internal engine bucket identifier, never shown to a user. This also exposed a real duplicate now that "Laser Cut" is on `co2_laser`: the long-dormant "Co2 Laser Cutting" catalog operation (phantom since migration 024, 0 real `process_cost_records`, no calculator) is the exact same shape of duplicate migration 715 already fixed once for "Fiber Laser Cut" — closed by migration 728 (deactivates it, reverts migration 725's own taxonomy auto-promotion for it back to `machine_class=NULL`/`roadmap_status='not_modeled'`, leaves Quattro's own `co2_laser` machine identity untouched). **Confirmed live (2026-09-10):** the "CO2 Laser Cutter" category rename had already reached the live database (HR Rates page screenshot: category sidebar reading "CO2 Laser Cutter (24)", Quattro among its rows) before this decision was reversed — migration 729 renames it back to "Laser Cutting Machine", matching the "[Technology] [Cutting] Machine" convention every sibling category already uses (Fiber Laser Cutting Machine, 3D Laser Cutting Machine, Oxyfuel/Plasma/Waterjet Cutting Machine, ...). **User asked for the stronger cut (2026-09-10):** not just deactivated (728) but removed end to end — migration 730 supersedes 728 for this operation with an outright `DELETE` from both `process_calculator_mappings` and `process_taxonomy`, so it no longer appears on the Process page at all (25 ops → 24), even as an inactive pill. A real live run of the first draft surfaced a genuine second blocker beyond the one migration 725 already knew about: `mhr_records.canonical_process_id` (migration 611) also FKs to `process_taxonomy`, and the 24 real "Laser Cutting Machine" rows were still carrying migration 569's original `operation='Co2 Laser Cutting'`/`process_route='Sheet Cutting'` — never repointed by 722/723, which only ever touched `machine_class`. Migration 730 repoints those 24 rows to the real, active identity (`operation='Laser Cut'`, `process_route='Laser Cutting'`, matching "Fiber Laser Cut"'s own row) before either delete, clearing both FKs. Migrations (722/723/727/728/729/730) written, not yet run (user runs migrations against the live DB).
- [x] Waterjet
- [x] Turret Punching / Nibbling
- [x] Bend Brake
- [x] OxyFuel Cut — real cost engine (`oxyfuel-cutting-engine.ts`), 18 real machines, real `sm_reference_data` feed-rate/pierce-time data (2026-09-01)
- [x] Shearing — real cost engine, reuses `computePressStrokeCost` (2-cut-per-blank Euclidean model), 10 real machines (2026-09-01)
- [x] Laser Punch — real cost engine, real per-machine punch/nibble physics, 26 real machines (2026-09-01). Fixed a real, pre-existing bug in the same pass: 127 of these machines were mislabeled `machine_class='turret_punch'`, silently contaminating the Turret Punch quote pool.
- [x] Plasma Cut — real cost engine (feed-rate/pierce-time model, not punch-cycle — the real data available determines the model), 13 real machines spanning 100W-100,000W, real per-machine power resolved before the nearest-power/nearest-thickness table lookup (2026-09-01)
- [x] Plasma Punch — same model as Plasma Cut (real data has no punch-cycle fields for this class despite the name), 12 real machines spanning 30W-400W (2026-09-01)
- [x] Standard Press, Tandem Press, Progressive Die — real cost engine (`press-stroke-engine.ts`, one shared "one stroke = one part" formula, 3 registered classes); Progressive Die uses 14 of 26 real machines (12 excluded — confirmed cross-category press-force contamination, same discipline as migration 608's Standard/Tandem split)
- [x] 2/3/4 Roll Bending — real cost engine (`roll-bending-engine.ts`, feed-length/rolling-speed model off `flatPatternLengthMm`), 25/19/4 real machines. Real-geometry capability gate added 2026-09-09 (`rollBendingGeometryCapability`, see Cost section) — this was previously "no cost engine yet"; that was stale.
- [x] Cut To Length Line (CTL) — real cost engine (`cut-to-length-engine.ts`), 8 real machines (40 `mhr_records` rows across 5 locations). FIXED 2026-09-10, closing migration 572's own confirmed "true gap with nothing to link": zero `process_calculator_mappings` row and every real row's `machine_class` was NULL. Real, uniform per-category linear formula (`const_coeff_cycle_time`/`mass_coeff_cycle_time`/`cut_speed_s` + reused `handling_time_const_s`/`handling_time_mass_coeff_s_per_kg`, migration 608's own shape) — identical across all 8 real machines, not a per-machine fit; real per-machine capability (thickness 1.75-12mm, coil width 1219-2438mm) stays ungeneralized (no class-wide floor), same discipline as shear/laser_punch. Migration 724 written, not yet run.
- [ ] Generic Press — confirmed genuinely absent from all real data sources (no machine category exists anywhere); deleted from the catalog entirely, not a gap to fill

**Operations** — each process should resolve to its own real sub-operations dynamically (e.g. Fiber Laser → Profile Cut / Hole / Complex Hole / Bevel; Bend Brake → Straight Bend / Multi Bend / Form / Flange), not a flat undifferentiated cost per process.

**Machine** — each operation should answer a real capability question ("can machine X perform operation Y on material Z at thickness T?"), never `process_name == machine_name` string matching.
- [ ] 3 of 7 USA fiber_laser `mhr_records` rows have zero thickness/bed capability data (documented gap, not fabricated: real spec exists in staged reference data for 2 of the 3, but shaped as unlabeled generic thickness tiers with no material-family legend — see `laserThicknessLimit`'s doc comment, `machine-selection/selector.ts`). As of the P0.7 fail-closed fix, `isCapable` now correctly rejects these rows as NOT capable on thickness instead of assuming capable — the data gap still needs sourcing, but it no longer produces a false-positive quote. Needs the real tier legend sourced before the 2 machines with staged-but-unmapped data can be backfilled.

**Machine Economics** (new architecture initiative, 2026-08-22, not yet started) — `mhr_records` today flattens machine identity + capability + economics + provenance into one table, seeded by a mix of real imports, name-matched seed guesses, and generic class defaults with no way to tell them apart at the DB level (see the `capability_source` column, which is inconsistently populated). Planned direction (audit before implement, same discipline as every P0.x phase): separate reference/benchmark data, real Digital Factory machine identity, and shop-specific economics into distinct layers, with one authoritative `resolveMachineEconomics()` resolver (customer/shop override → Digital Factory actual rate → approved company benchmark → industry benchmark → generic fallback) instead of every consumer reading `mhr_records` columns directly. Quote/cost snapshots must keep remembering the resolved value at time of apply (mirrors the existing FX snapshot pattern) so a later rate change never silently changes a historical quote. `memory/sheetmetal/machine/machine_library.json` is reference/provenance data feeding this pipeline, not something a UI should read directly to auto-fill a form.

**DFM** — `dfm-scoring.service.ts` is the sole manufacturability-verdict authority (P0.3); UI surfaces consume its result, never re-derive their own threshold.
- [ ] DFM persistence/audit-trail (documented gap, not started: DFM has no storage layer at all today — every `GET /bom-items/:id/dfm-scores` call fully recomputes live from the item's current feature_graph/thickness/material and writes nothing back; no `dfm_scores` table, no rule-version stamp. This is fine as long as nothing needs "what did DFM say at time X" — only build real persistence + rule-versioning together, deliberately, if that requirement ever arrives.)
- [ ] `process-planning/page.tsx`'s `DEFAULT_DFM_FEATURES`/`DFMColorMesh` surface is fully disconnected from the one DFM authority above (P0.3, deferred) — a second, independent DFM-adjacent judgment exists on that page.

**Cost** — feature → operation → process → machine → parameters → time → rate → cost, with an explainable calculation trace, and no hard-coded per-part-scenario logic.
- [x] Press-family (Standard Press/Tandem Press/Progressive Die) and Roll Bending routes double-charge bending — FIXED (2026-09-04). Was: `getRouteComparison()` appended a separate real Press Brake line to every route whenever `bendCount > 0`, cutting or forming alike. Initial read treated this as needing new die-station data before it could be fixed (removing it looked like it would silently zero-price the bending work) — that read was wrong: the forming route's own primary process line (Standard Press/Tandem Press/Progressive Die/Roll Bending's real `computePressStrokeCost()`/`computeRollBendingCost()` cost, driven by that machine's real cycle time and rate) already represents "this press/roll produces the finished formed part" — it was never a $0 line, so a separate Press Brake charge on top was double-counting a real bending operation, not filling a real pricing gap. Real catalog taxonomy (`process_calculator_mappings`, verified directly against `process_operations.json`: `Std Press:Std Press//StraightBend`, `Tandem Press:Bending//StraightBend`, `Progressive Die:Die Station:Bending//StraightBend`, `2/3/4 Roll Bending:...//StraightBend`) confirms all 6 forming classes are registered as performing bending in their own process — a real, sourced signal, not fabricated. Fix: `shouldAddSeparatePressBrakeLine(processFamily)` (`engine-kernel.ts`, 2 tests) — `assembleRoute()` in `getRouteComparison()` now only appends the Press Brake line for `processFamily === 'cutting'` routes; forming routes get a disclosure warning instead ("Bending performed in-process...") when a bend was suppressed. 512 backend tests green throughout, no other cost line touched.
- [x] Roll Bending had no capability check at all, so it could win route comparison for a part with ordinary discrete press-brake bends — FIXED (2026-09-09). Reported case: 830-001720-00 (SECC, 1.5mm, four discrete R0.8mm bends) was ranked/applied as "3 Roll Bending" instead of a cutting route + Press Brake. Root cause: `computeRollBendingCost()` takes `flatPatternLengthMm` as its roll feed length, and every sheet part has a flat-pattern length, so a roll bender always produced a plausible cycle time and always appeared as a candidate route — nothing checked whether the part's formed geometry (N discrete creases) is something a roll bender can physically produce (one continuous curvature, not discrete bends). Compounding it, `getRouteComparison()` priced a roll-bending route as a complete standalone alternative with no blanking cost at all (`2/3/4 Roll Bending` has no `//Blank` operation in the real catalog taxonomy — "a roll bender feeds an already-cut blank between rolls"), so it won on price for a reason that was not economic. Fix: two functions in `engine-kernel.ts` — `rollBendingGeometryCapability(machineClass, bendCount)` bars `roll_bending_2/3/4` whenever `bendCount > 0`, with a real reason string; `routeProducesBlank(machineClass, blankCapableClasses)` (sourced from `process_taxonomy_operations`, migration 609/610, not a hand-kept list) flags that a route has no blanking op of its own. Both feed `RankableRoute.isFeasible`/`producesBlank` in `assembleRoute()` (`bom-items.service.ts`), which `selectRecommendedRoute()` and the "Lowest cost" badge now gate on — a disqualified route stays visible, costed, and manually selectable, with its warning shown, but is never auto-recommended or badged cheapest. Regression test names the real part (`engine-kernel.spec.ts`, "THE REPORTED REGRESSION (830-001720-00...)"). **Update (2026-09-09, same day): the one-directional-gate limitation noted here is now closed.** `cad-engine/sheet_metal/features/rolled_form.py` (15 real B-Rep tests) reads the raw cylinder tuples *before* `_collect_dedup_bends`' rejection gates and reports `recognition_status='recognized'` whenever a single cylindrical face sweeps past 180° — a press brake physically cannot wrap material past a half-turn in one hit, so this is a real, separable positive signal (a large-radius face sweeping *within* press-brake range still correctly stays `'ambiguous'`, disclosed not guessed). Wired into `extract()` (`feature_extractor.py`, emits `rolled_form_count`/`rolled_form_candidates`, additive — every `bend_*` field is unchanged) and end-to-end into the backend: `auto-fill.service.ts` maps it to `rolledFormCount`; `rollBendingGeometryCapability()` gained a third `rolledFormCount` argument and is now `capable: true` whenever a real rolled form is detected, even alongside discrete bends elsewhere on the same part (a rolled shell with a flanged edge is a real, ordinary combination); a new `rolledFormNeedsRollBender()` flags the reverse case — a cutting-only route is now warned when the part has a real rolled form it can't produce. Both wired into `assembleRoute()`, both covered in `engine-kernel.spec.ts`. 90 cad-engine tests green (via the project's WSL2/Docker runtime), tsc clean, full backend suite green throughout.

### Testing philosophy for readiness

Beyond unit tests, maintain a **Sheet Metal scenario matrix** — real material/thickness/geometry/process combinations, including combinations not explicitly coded for, to prove the engine derives behavior rather than pattern-matching known scenarios.

### Freezing a domain

Once Sheet Metal reaches this level of readiness:
1. Freeze the Sheet Metal v1 architecture and contracts (feature contract, operation model, process model, capability model, machine model, costing interfaces, calculation trace, test suite).
2. Preserve its regression/scenario suite.
3. Avoid unrelated refactors to frozen domains.
4. Begin the next domain (Machining) as its own separate vertical.
5. Reuse the proven *engineering philosophy* (CAD → features → DFM → operations → process → capability → machine → cost), not sheet-metal-specific assumptions — Machining and Injection Molding will have genuinely different feature/domain models (holes/pockets/slots/threads/tool-changes for Machining; parting lines/draft/wall thickness/gates/runners/cooling/tonnage for Injection Molding).

Only make changes to a frozen domain for a real production bug or a genuinely required new capability — not exploratory redesign.

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
